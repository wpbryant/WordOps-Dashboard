---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/main.py, backend/config.py, backend/requirements.txt, pyproject.toml]
autonomous: true
---

<objective>
Set up Python/FastAPI project structure with working development server.

Purpose: Establish the foundation for all subsequent backend development.
Output: Runnable FastAPI application with health endpoint, CORS configured.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project structure and dependencies</name>
  <files>backend/requirements.txt, pyproject.toml, backend/__init__.py</files>
  <action>
    Create project directory structure:
    - backend/ directory for Python code
    - backend/__init__.py (empty, makes it a package)
    - requirements.txt with: fastapi>=0.109.0, uvicorn[standard]>=0.27.0, python-jose[cryptography]>=3.3.0, passlib[bcrypt]>=1.7.4, pydantic>=2.0, pydantic-settings>=2.0
    - pyproject.toml with project metadata (name: wordops-dashboard, version: 0.1.0, python: >=3.10)

    Do NOT install dependencies - just create the files. Installation happens at execution time.
  </action>
  <verify>ls backend/requirements.txt pyproject.toml backend/__init__.py</verify>
  <done>Project structure exists with dependency files</done>
</task>

<task type="auto">
  <name>Task 2: Create FastAPI application skeleton</name>
  <files>backend/main.py, backend/config.py</files>
  <action>
    Create backend/config.py:
    - Use pydantic-settings BaseSettings for configuration
    - Settings: SECRET_KEY (str), DEBUG (bool, default True), CORS_ORIGINS (list, default ["*"])
    - Load from environment variables with WO_DASHBOARD_ prefix

    Create backend/main.py:
    - FastAPI app instance with title "WordOps Dashboard API", version "0.1.0"
    - CORS middleware allowing configured origins (methods: GET, POST, PUT, DELETE, OPTIONS)
    - GET /health endpoint returning {"status": "healthy", "version": "0.1.0"}
    - GET /api/v1/health same response (API versioned path)

    Use async def for route handlers. Import Settings from config.
  </action>
  <verify>cd /home/william/Projects/WordOps-Dashboard && python3 -c "from backend.main import app; print(app.title)"</verify>
  <done>FastAPI app imports without errors, has health endpoint</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 -c "from backend.main import app"` succeeds
- [ ] `python3 -c "from backend.config import Settings"` succeeds
- [ ] All files exist in expected locations
</verification>

<success_criteria>

- All tasks completed
- FastAPI application skeleton ready for development
- Configuration system in place
- Health endpoint defined
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
